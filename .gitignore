# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Downloaded AI models (large, auto-downloaded on first use)
models/*.tflite

# Temp files from /prepare endpoint
tmp/

# Output files (keep UI asset folders)
*.webp
!zoom_images/*.webp

# Release artifacts (upload to GitHub Releases, don't commit)
*.zip
*.flatpak

# Pre-downloaded pip wheels for Flatpak build (generated, not committed)
pip-wheels/

# OS
.DS_Store
Thumbs.db
